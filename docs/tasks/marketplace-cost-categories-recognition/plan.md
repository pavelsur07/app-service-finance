# marketplace-cost-categories-recognition: распознавание новых категорий затрат Ozon и WB

Бриф Владельца 26.09.2026: найти категории затрат, которые на проде приходят
нераспознанными, и добавить их в модуль Маркетплейс.

Решения Владельца: `TemporaryPlacement` → `ozon_temporary_storage`;
«Добровольная выплата за товары» WB остаётся вне ОПиУ; разметку справочника Ozon
делаем заранее в этом же PR.

stage_base_commit: 85038031 · риск: HIGH-LOCAL (миграция данных, классификация затрат)

## Замеры на проде до изменения (26.09.2026, read-only)

- `ozon_unknown_52` (PremiumSubscription): 3 строки, 59 970,00 руб., 3 компании,
  11–25.09; легаси-путь с января клал те же 24 990 / 9 990 в `ozon_premium_promotion`.
- `ozon_unknown_78` (TemporaryPlacement): 2 строки, 300,00 руб., 1 компания, 20.09.
- Все 5 строк: `document_id IS NULL`, дата позже `finance_lock_before` компании;
  привязок ОПиУ у неразобранных категорий нет; в целевых категориях строк с
  `external_id` `%-type-52` / `%-type-78` — 0.
- WB: `wb_okazanie_uslug_wb_media` 1 строка 3 324 руб. — ни в каталоге, ни в yaml;
  «Джем» 101 157 руб. и «Баллы за отзывы» 3 864,96 руб. — в yaml, но не в каталоге.

## Stage 1 — код и данные

1. Ozon: 16 имён by-day в `OzonCostCategory::$accrualTypeNames` (2 с деньгами на проде
   + 14 заранее, у каждой целевой категории есть деньги легаси-путём и правило ОПиУ).
2. Ozon: `OzonCostCategoryTest::AWAITING_CLASSIFICATION` — 74 услуги справочника,
   намеренно оставленных неразобранными; тест падает на услуге без решения.
3. WB: три кода в `WbCostCategory` под теми же кодами, что даёт слугификация;
   правило `wb_okazanie_uslug_wb_media` → `PROMO_INTERNAL`.
4. Миграция `Version20260926120000`: перенос 5 строк в категорию каталога той же
   компании (только вне документа ОПиУ и вне заблокированного периода), мягкое
   удаление опустевших `ozon_unknown_52/78`; обратимая.

DoD: регрессионные тесты красные на старом коде; гейты зелёные; baseline не растёт.
