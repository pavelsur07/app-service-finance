# Checkpoint — остатки в аналитике Маркетплейса

| | |
|---|---|
| Ветка | `feat/inventory-stock-freshness` |
| PR | [#2430](https://github.com/pavelsur07/app-service-finance/pull/2430) — **смёржен 2026-09-06**, merge-коммит `e575a627` |
| `stage_base_commit` | `8f034153` |
| Коммит Stage 1 | `0ea041da` |
| Коммит Stage 2 | `e2b195d2` |
| Коммит Stage 3 | `da2d599e` |
| Обновлено | 2026-09-06 |

## Состояние

| Stage | Статус | Гейт |
|---|---|---|
| 1 — Свежесть остатков | **Завершён**, `REVIEW_GREEN` | `owner_gate: no` |
| 2 — Остатки как источник строк отчёта | **Завершён**, `REVIEW_GREEN` | `owner_gate: yes` — **требуется решение** |
| 3 — «Нет данных» вместо нуля | **Завершён**, `REVIEW_GREEN` | `owner_gate: yes` — **требуется решение** |

## Пере-нормализация выполнена на проде — 2026-09-06

`app:inventory:renormalize-snapshots --from=2026-05-11 --to=2026-09-05 --source=ozon --execute`
отработала: 310 сессий, `errors count: 0`. **6231 строка получила `listing_id`**,
непривязанных осталось 567.

Инварианты целы: число строк, суммы количеств и резервов не изменились ни на
единицу, ни одна существовавшая привязка не потеряна — сверено построчно по всем
86 899 строкам с копией `inventory_stock_snapshots_bak_20260906`. Подробности —
`stages/stage-4.md`, раздел «Прогон на проде».

Копию таблицы и дамп `/root/isb.sql.gz` удаляет Владелец, когда результат устроит.

## Что уже действует после Stage 1–3

Протухший снапшот (старше 2 дней) не участвует в расчёте остатков. Для компании
с таким снапшотом остаток в отчёте показывается как `0` — промежуточное состояние,
прочерк появится в Stage 3.

Листинг с остатком > 0 даёт строку в юнит-экономике даже без движения за период:
на PROD это **177 новых строк** по 4 компаниям. Остатки отфильтрованы по
маркетплейсу вместе с остальными источниками.

Неизвестный остаток больше не показывается нулём: в таблице прочерк, в XLSX пустая
ячейка, в JSON `null`. Свод по тегам суммирует только известные значения и сообщает,
сколько листингов в сумму не вошло. В ответе есть дата снапшота и список отброшенных
источников.

Гейт `app:inventory:stock-freshness-check` существует, но **в cron не поставлен**.

## 🔴 Hotfix 1 — 2026-09-06, закрыт

После деплоя `e575a627` в отчёте юнит-экономики **обнулились все остатки и пропала
капитализация**. Причина: порог свежести из Stage 1 считался от даты отчёта, а
пресет «текущий месяц» отдаёт последний день месяца — дату в будущем. Свежий
снапшот оказывался «на 24 дня старше» и отбрасывался.

Исправление — свежесть от `min(дата отчёта, сегодня)` — в релизе `7b4328cc`.
Разбор: `stages/hotfix-1.md`.

**Подтверждено на проде.** Для параметров из жалобы
(`marketplace=ozon`, `dateTo=2026-09-30`, ИП Лазарева `3f4d87cc`) запрос отдаёт
снапшот за 06.09: **242 листинга с остатком, из них 228 с ненулевым, суммарно
2105 шт**. Владелец подтвердил: остатки появились. Новых ошибок в GlitchTip нет.

### Ожидаемое следствие, которое легко принять за дефект

Колонка «Остаток на МП» у большинства строк показывает прочерк. За сентябрь у этой
компании продажи есть у 31 листинга, а остаток — у 228. Капитализация считается как
`остаток × себестоимость единицы`, себестоимость берётся из продаж периода — у 197
позиций её попросту нет.

Раньше там стоял ноль, и он утверждал «капитала нет» при товаре на складе. Прочерк
честнее. Взять себестоимость больше неоткуда: справочник товаров на проде пуст
(2 товара на 1410 листингов), а закупочные цены живут там. Это упирается в разрыв
«листинг → товар», снятый Владельцем с повестки в начале задачи.

## Проверка после прода — 2026-09-06

Деплой прошёл успешно, прод на образе `site-php-cli:e575a627`. Миграций в релизе нет
(`migrations: skipped`), схема не менялась.

**Гейт свежести прогнан на проде своей командой**, а не репликацией SQL:

```
checked pairs count: 7
stale count: 0
missing count: 0
max age days: 2
finish
```

Все 7 пар «компания + источник» — `OK`, снапшот за текущий день. **Exit code 0**,
значит ночной прогон по cron алерт не поднимет. Гейт достижимо зелёный не в теории,
а на реальных данных.

Инварианты целы, ничего не сдвинулось:

| Показатель | Значение |
|---|---|
| Failed-очередь Messenger | 17 — столько же, сколько до работы |
| Строк `inventory_stock_snapshots` | 86899 за 119 дней, последний день 2026-09-06 |
| Сумма количеств | 4026854.000 |
| Сессий не в статусе `completed` | 2 — исторические, от 10.05 и 13.07 |

**GlitchTip:** новых ошибок после деплоя нет — самый свежий issue датирован
2026-09-05T11:17, то есть вчерашним днём. Все 13 незакрытых issue унаследованные и к
изменениям не относятся (Ozon accrual metadata, 404 от Ozon Performance, messenger
handler failures, известный `ProcessSignaledException` планировщика). Записей про
`MarketplaceAnalytics`, `UnitExtended` или `Inventory` в списке нет.

Это **слабое доказательство**: с деплоя прошло около 15 минут, и если отчёт за это
время никто не открывал, страница просто не выполнялась. Сильная проверка — открыть
сам отчёт юнит-экономики и убедиться, что таблица рисуется, а в колонках остатка и
себестоимости появились прочерки; после этого повторить `./gt.sh list`.

Первый автоматический прогон гейта по расписанию — завтра в 05:30.

## Следующие шаги

1. ~~Прогнать гейт на проде read-only и убедиться, что он зелёный.~~ **Выполнено
   и подтверждено настоящим прогоном команды, см. «Проверка после прода».** Сама команда на прод не деплоилась (она только в этой ветке, прод
   собран из `master`), поэтому её логика воспроизведена один в один через
   `codex-psql-ro`. Результат: **7 из 7 пар OK**, возраст снапшота 0 дней, ни одной
   `STALE` или `MISSING` — гейт достижимо зелёный.

   Охват проверки подтверждён отдельно: у `5d26f9aa` (снапшот 2026-05-23, 106 дней)
   **нет ни одной строки в `marketplace_connections`** — ни активной, ни выключенной.
   Загрузка для неё не запускается, чинить нечего, и гейт её справедливо не видит.
   Всего в базе 11 подключений, все активные; выключенных нет вовсе.
2. После подтверждения — поставить гейт в cron. Предусловие выполнено, изменение
   `docker/cron/app.cron` не вносилось: это правка деплой-конфигурации, отдельное
   решение Владельца.
3. **Решение Владельца по Stage 2** (`owner_gate: yes`): отчёт видимо меняется на
   177 строк. До решения PR остаётся Draft.
4. ~~**Решение Владельца по конфликту с `CLAUDE.frontend.md` §16.1**~~ — **принято
   2026-09-06: мержим как есть**, конфликт остаётся зафиксированным исключением.
   Откат Stage 3 и 5 не требуется. Миграция `_legacy` выносится в отдельную задачу.

## Открытые вопросы к Владельцу

- Внешних потребителей API нет — подтверждено 2026-09-06, поэтому `stockQty: number | null`
  в Stage 3 не является ломающим изменением за пределами репозитория.
- Поведение сумм в `TagSummaryTable` при неизвестных значениях — решать в Stage 3.
- ~~**`costPriceUnit` остаётся `0.0` при неизвестной себестоимости**~~ — **закрыто
  Stage 5** (`stages/stage-5.md`): поле стало nullable, различие «известный ноль» и
  «неизвестно» доведено до контракта и покрыто парой тестов.
- **Миграция `marketplace-analytics` из `_legacy` в `modules/`** — отдельная задача,
  не предусловие этого PR. Оценка объёма (2026-09-06): модуль 35 файлов / 4399 строк,
  это 44% всего легаси-React (9911 строк). Не автономна: модуль зависит от
  `_legacy/shared` (9 файлов, 854 строки), который используют ещё 22 файла четырёх
  других модулей, а ESLint-границы запрещают и `modules/ → _legacy`, и
  `shared/ → _legacy`, и `_legacy → modules/`. Отсюда развилка: нарушить §16.4 двумя
  копиями shared либо тащить весь `_legacy` разом. Целевой инфраструктуры нет вовсе
  (`modules/`, `entrypoints/`, `app/providers.tsx`, `mountIsland`; в `shared/` один
  файл, в React-обёртках UI Kit только `Button`). Задевает **6 обязательных STOP**
  из `CLAUDE.frontend.md` §6. Страховки нет: **0 фронтенд-тестов** в репозитории и
  нет скриптов `test`/`typecheck`. **Начинать следует с `shared/` и каркаса**, а не
  с этого модуля.

## Вне scope задачи

Связь `listing → product`, авто-создание листингов из остатков, backfill
исторического `listing_id`, 3 строки WB `unmapped`, позиции Ozon с пустым `stocks`.
Подробности — в `plan.md`, раздел «Вне scope всей задачи».
