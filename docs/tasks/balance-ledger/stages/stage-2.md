# Stage 2 — Balance interface

Risk: HIGH-LOCAL. Integrated task base: `238dd997`. Implements the approved frontend work package 4; backend and frontend delivery commit is consolidated as explained in Stage 1.

## Implemented

- Twig and Symfony Forms screens for setup, explicit default structure creation, four-level article tree, account maintenance, current balances, target correction and recalculation of an existing target draft.
- Document editor with dynamic lines and exact BigInt preview, save/post/delete/reverse, explicit zero-opening confirmation, reviewed version submitted to the atomic posting command.
- Journal filters and pagination, dated hierarchical balance, account/article cards, statement, comparison, periods, owner-managed grants and audit.
- Separate closing/reopening permissions, company-scoped lookups, CSRF on every mutation, owner-only audited state rebuild form with required reason.
- Existing `/balance/` and `/balance/structure/` entry routes retained. Legacy financial linking routes return 410; obsolete linking buttons removed.
- Hidden report rows do not change totals; descendant categories are excluded from parent selection. Existing helpers, money formatter, Pagerfanta and UI Kit are reused; no dependencies or custom CSS added.

## Final focused verification

- `/tmp/vf-balance-run php bin/phpunit tests/Functional/Balance`: **26 tests, 170 assertions**, green (27.367 seconds).
- `/tmp/vf-balance-run php vendor/bin/phpstan analyse src/Balance/Controller src/Balance/Form tests/Functional/Balance --no-progress --memory-limit=1G`: no errors.
- CS Fixer using the actual gate configuration `.php-cs-fixer.php`, scoped to the same paths: final dry-run **0 of 35 files** require fixes.
- Twig lint of `templates/balance`, `templates/balance_structure` and `templates/balance_error.html.twig`: **23 files**, green.
- Project UI Kit checker restricted to those changed templates: **23 files, 312 usages**, all classes found. Global checker has the recorded legacy baseline of 8,965 violations; no global-clean claim is made.
- Focused Stimulus ESLint passed; full npm lint and Vite build were run successfully by the root agent. Global UI Kit React mapping baseline is recorded in the handoff; no React mapping contract changed here.

## Browser and behavioral evidence

Authenticated BrowserKit tests cover setup, empty screens, structure validation, balanced opening through actual forms, report/card results, account creation, foreign-company denial, read-only visibility and CSRF rejection. Regressions also cover selected-account target balances, target refresh preserving the draft, hidden rows preserving totals, malformed dates, pagination bounds and sparse invalid document rows.

Playwright loaded an authenticated editor HTML artifact generated by BrowserKit with the real compiled assets through a temporary local HTTP server. At 1440px and 390px, adding/removing a line worked, entering `123.45` displayed exact `123,45`, no horizontal viewport overflow occurred and no browser errors were reported. Screenshots were visually inspected. This verifies rendering and Stimulus interactions, not a separate login E2E: the initial direct login smoke bounced and was replaced by the authenticated artifact. Temporary browser containers and HTTP servers were stopped; synthetic fixtures/screenshots remain ignored local artifacts.

## Review disposition

- External BLOCKER: selected target account overwritten by a loop variable. Fixed; regression proven red then green with two distinct balances.
- Internal IMPORTANT: sparse line indices could overwrite a surviving line after invalid submission. Fixed by deriving the next index from the maximum rendered key; regression green.
- Internal financial review fixes consumed by UI: reviewed version and explicit zero-opening flag passed into atomic posting, separate reopening grant, paginated cards with complete opening/closing values, owner recovery form.
- Safe MINOR fixes: exclude descendants from parent choices, honor row visibility, use hierarchical category names, bounded list pages, and valid UI Kit error wrapper.

No known UI blocker remains. Whole-task internal/external review disposition and final full gates are maintained by the root agent in the handoff; this report does not claim those pending review rounds are green.
