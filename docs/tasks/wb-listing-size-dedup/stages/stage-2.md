## Stage 2: существующие дубли слиты — DONE

**Риск:** 🟠 HIGH-LOCAL (миграция по денежным данным; прогон на проде — только Production Gate)
**Owner gate:** yes
**Release candidate:** yes
**Independently deployable:** no
**Следующее действие:** 🛑 STOP, Release Gate — решение Владельца по merge и отдельно по прогону на проде

### Scope Stage
- Stage base commit: Stage 1 head
- Work items completed: `2.1`, `2.2`, `2.3`, `2.4`

### Что сделано
- Миграция `Version20260728120000`: для каждого `(company_id, nm_id)` WB с половинами `size='0'` и `size='UNKNOWN'` выбирается канонический листинг (самый старый по `created_at`), на него переключаются все 14 таблиц с `listing_id`, дубль удаляется, `size='0'` нормализуется в `'UNKNOWN'`.
- Конфликты уникальных ключей разрешаются в пользу канонического: себестоимость `(listing_id, effective_from)`, теги `(listing_id, tag_id)`, дневные срезы `(company_id, listing_id, snapshot_date)`, рекламные затраты `(company_id, listing_id, date, advertising_type, external_campaign_id)`.
- Строки рекламных документов `(ad_document_id, listing_id)` **суммируются** (`cost`, `share_percent`, `impressions`, `clicks`), а не удаляются — иначе терялся бы рекламный расход. На проде таких пересечений 212.
- Идентичность дубля (chrtId, `product_id`, `supplier_sku`, `name`) переносится на канонический листинг после удаления дубля — иначе частичные уникальные индексы на `marketplace_variant_id` и `product_id` не дали бы её скопировать.

### Затронутые файлы
- `site/migrations/Version20260728120000.php` — new

### Self-review
- [x] Scope compliance — только миграция
- [x] Полнота списка таблиц: перечислены все таблицы схемы с колонкой `listing_id` (14)
- [x] Порядок шагов: конфликты → перенос детей → удаление дублей → перенос идентичности → нормализация size
- [x] Company-scope: слияние идёт внутри `(company_id, marketplace_sku)`, чужие компании не смешиваются
- [x] Ozon и листинги с настоящими размерами не затрагиваются
- [x] `down()` — `IrreversibleMigrationException`
- [x] CS-Fixer точечно / `php -l` — green
- [x] ARCHITECTURE.md — N/A

### Проверено на локальной БД
Сценарий: пара-двойник (A `size='0'`, старее, 2 продажи, себестоимость на 01.06, баркод, рекламная строка 100 ₽; B `size='UNKNOWN'`, chrtId=777, 1 продажа, себестоимость на ту же дату 01.06 + на 01.05, баркод, рекламная строка 40 ₽) плюс контроли: WB с настоящим размером `M`, одиночный WB `'0'`, Ozon `'0'`.

Результат после `doctrine:migrations:migrate`:
- остался один листинг WB 900001: `size='UNKNOWN'`, chrtId `777`, `supplier_sku` и `name` подтянуты с дубля;
- продажи 3/3 на каноническом; себестоимость 2 записи — конфликтная дата дубля удалена (осталась `111.00` канонического), уникальная `333.00` переехала;
- баркоды 2/2 на каноническом;
- рекламная строка одна: `cost=140`, `impressions=300`, `clicks=30`, `share=100` — сумма документа сохранена;
- контроли не тронуты: `M` остался `M`, одиночный WB `'0'` → `'UNKNOWN'`, Ozon `'0'` остался `'0'`.

Тестовые данные из локальной БД удалены после проверки.

### External Claude Code review
- Result: N/A — см. Stage 1 (реализацию ведёт сам Claude Code). Выполнен внутренний review полного Stage diff.

### Команды для проверки
- `docker compose run --rm site-php-cli php bin/console doctrine:migrations:migrate --no-interaction`
- `make site-test-unit`

### Риски / на что обратить внимание ревьюеру
- Миграция необратима: удалённые дубли и слитые рекламные строки не восстановить. Перед прогоном на проде — бэкап затронутых таблиц.
- Затрагиваются денежные данные: продажи 3925, затраты 27240, рекламные строки 220, себестоимость 10, баркоды 168, возвраты 7 (замер на 28.07.2026).
- Себестоимость продаж после слияния не пересчитывается — решение о `RecalculateSalesDocumentsCostPriceAction` отдельное.

### Открытые вопросы
- Пересчитывать ли себестоимость WB-продаж после слияния — решение Владельца.
