Stage1 only. Specification: docs/tasks/api-management/TASK.md and plan.md. UI and prepared permissions are intentional later Stages; current principal returns empty scope arrays. Financial endpoints are out of scope. Review backend auth/key Actions/company IDs/audit/limiting/secrets.

Internal findings fixed and verified: UTC expiry hydration drift fixed with PostgreSQL TIMESTAMP WITH TIME ZONE + datetimetz_immutable and UTC response; exact expiration boundary test. Two fresh-review IMPORTANT: valid-secret malformed company header now consumes key budget before422;61st invalid-header request429 proven red/green. Revoke now company-scoped SELECT FOR UPDATE with HINT_REFRESH inside transaction so a waiter observes existing revocation and produces no duplicate audit; stale entity OptimisticLockException regression red/green. OpenAPI error content corrected to application/problem+json.

Company public ID migration tested in isolated schemas with populated companies, old INSERTs omitting column, distinct IDs on concurrent physical PostgreSQL connections and nonreuse after rollback. Shared local test DB had0 companies before migration; only new task migrations applied; no production access/data. UUID references unchanged. Down explicitly forward-fix since published IDs/key revocations must not be reused.

Internal scoped re-review open0 BLOCKER/IMPORTANT/MINOR. Api lifecycle unit+real DB tests18/63; external auth12 tests including rate bypass; Company public ID/Facade11/35. Broader Stage module check rerunning after timezone assertion adjustment (naive timezone-string assertion was fixed to compare epochs). No new dependencies. Existing UI Kit baseline9107 legacy violations before any UI change.
