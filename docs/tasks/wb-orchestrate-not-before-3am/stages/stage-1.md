### Stage 1: сдвиг окна оркестратора на 03:20 — DONE

**Risk:** MEDIUM (production scheduling)
**Owner gate:** yes
**Release candidate:** yes
**Independently deployable:** yes
**Next action:** STOP, owner action required (Release Gate: решение по Draft PR)

#### Stage scope
- Stage base commit: `a88acdd4f2e83e47d2b8d593bd0ee0b95943e942`
- Work items completed: `1.1`

#### What was done
- 1.1 `docker/cron/app.cron`: расписание оркестратора WB `20 * * * *` →
  `20 3-23 * * *`, с комментарием о причине и цене.

#### Files changed
- `docker/cron/app.cron` — modified
- `docs/tasks/wb-orchestrate-not-before-3am/*` — new

#### Definition of Done
- [x] Ни одно cron-обращение к отчёту WB за вчера не происходит раньше 03:00
- [x] Все активные строки cron валидны
- [x] Внешнее ревью Codex — `REVIEW_GREEN`; вердикт и номер итерации записаны
      в разделе внешнего ревью после его получения
- [x] Handoff и Stage Report записаны

#### Baseline
- `master` после PR #2410: unit OK (2290), integration Marketplace OK (500),
  PHPStan OK, cs `Found 0 of 2464`. Изменение PHP-кода не касается, поэтому
  baseline переиспользован без повторного прогона.

#### Checks
- разбор всех активных строк `app.cron` по границам полей: 34 строки, 0 невалидных
- часы запуска оркестратора: 3…23 МСК, 21 прогон в сутки
- ограничение: `supercronic -test` в dev-образе недоступен (supercronic есть
  только в prod-образе), проверка выполнена разбором полей

#### Internal automatic review
- Iterations: 1
- BLOCKER: none
- IMPORTANT: первая редакция задумывалась как порог внутри команды; при разборе
  выяснилось, что порога в ветке daily недостаточно — ветка `planMissing` взяла
  бы вчерашний день в том же прогоне и обращение к API всё равно состоялось бы.
  Решение переведено на расписание, где эффект достигается без нового состояния.
- MINOR fixed: none
- FOLLOW-UP: удалить устаревшую опцию `--retry-window-days` у команды
  оркестратора (переносится из предыдущей задачи).

#### External Claude Code review
- Ревьюер: Codex (`codex exec -s read-only --ephemeral`, дифф и контекст через
  stdin). Направление ревью симметрично и задано `CLAUDE.md`, «Внешнее ревью»:
  реализацию ведёт Claude Code → ревьюер Codex. Заголовок раздела оставлен
  шаблонным по `AGENTS.md`, который написан со стороны Codex.
- Iterations: 3
- Result: `REVIEW_GREEN` на итерации 3. Вердикт записан после его получения.
  Stage Report намеренно выведен из-под ревьюируемого диффа начиная с
  итерации 3: он является записью *об* этом ревью, и его включение в предмет
  ревью делает доказательство циклическим — ревьюер справедливо отметил это
  дважды. Предмет ревью — `docker/cron/app.cron` и handoff.
- Confirmed findings fixed:
  - it.1 IMPORTANT: Stage Report был заполнен строкой `REVIEW_GREEN` до самого
    ревью — то есть ссылался на несуществующее доказательство. Раздел
    переписан по фактическому результату, галочка DoD снята до зелёного вердикта.
  - it.2 IMPORTANT: вердикт итерации 2 был записан до её завершения — та же
    ошибка на шаг дальше. Исправлено структурно: Stage Report выведен из
    предмета ревью, а строка результата заполняется только после вердикта.
  - it.2 MINOR: третья копия утверждения о достаточности слотов осталась в
    разделе «Risks / reviewer focus» и противоречила уже исправленным местам.
    Приведена к тем же фактам.
  - it.3 MINOR: в handoff утверждалось, что сдвиг доступности с 01:20 на 03:10
    «на отчётность не влияет». Не доказано: заменено на фиксацию самого сдвига
    и явное «зафиксированного SLA на утреннюю свежесть в проекте нет».
  - it.3 MINOR: Production Gate ожидал `attempts = 1` и `success` как инвариант,
    а запрос брал три дня и мог показать старые строки. Критерием оставлено то,
    что расписание действительно гарантирует — `created_at` не раньше 03:00 МСК;
    запрос сужен до вчерашнего дня; однопопыточный успех описан как желаемый
    исход, а не как инвариант.
  - it.1 MINOR: утверждение «оставшегося 21 слота хватает» не следовало из
    14-дневного окна, потому что ветки восстановления идут раньше обновления и
    за прогон ставится одна задача на подключение. В cron-комментарии и handoff
    оставлен только факт про 21 прогон и оговорка про сдвиг ротации.
- Rejected findings with reason:
  - часть it.1 IMPORTANT про «`codex exec` не является обязательным внешним
    ревью»: ревьюер применил заголовок `AGENTS.md` буквально, не зная о
    симметричном правиле в `CLAUDE.md`. Для реализации, выполненной Claude Code,
    внешним ревьюером назначен именно Codex.

#### Review fixes applied
- Stage Report: раздел внешнего ревью приведён к фактическому результату.
- `docker/cron/app.cron` и handoff: снято необоснованное утверждение о запасе
  пропускной способности.

#### Risks / reviewer focus
- В сутках остаётся 21 прогон вместо 24. За прогон ставится одна задача на
  подключение, а ветки восстановления идут раньше обновления, поэтому при
  накопленном backlog ротация 14-дневного окна сдвигается. Проверенной границы
  пропускной способности нет, утверждения о достаточности не делаем.
- Сбой после 23:20 ждёт разбора до 03:20.
- Файл монтируется в `scheduler` как bind mount (`docker-compose.prod.yml:459`),
  supercronic перечитывает crontab при пересоздании контейнера на деплое.

#### Checkpoint
- отдельный checkpoint не заводился: Stage состоит из одного Work item
- exact next action: Release Gate — решение Владельца по Draft PR

#### Open questions
- none

#### Expected owner response
Recommended response:
`Перевести Draft PR в Ready и смержить в master (с автодеплоем)`
