# Checkpoint: cash-transaction-splits

Обновляется после каждого Work item.

## Текущее состояние

- **Фаза:** Stage 1 (схема, Entity, dual-write, аудит, backfill/verify).
- **Ветка:** `agent/cash-transaction-splits`.
- **stage_base_commit Stage 1:** `80ad3cfe26237824682f78526702fe7ee97649ff`.
- **Решения:** D1 снят (ограничение по `allow_pl_document`), D2–D4 подтверждены Владельцем 2026-08-01.

## Сделано

| Дата | Что |
|---|---|
| 2026-08-01 | Phase 0: план ревизий 1–3, `TASK.md`, разбор внешнего ревью |
| 2026-08-01 | PROD read-only срез: 6034 транзакции, 7 компаний, 20 категорий с ОПиУ, 0 мультистатейных разнесений |
| 2026-08-01 | 1.1 миграция `Version20260801160000` — таблица `cash_transaction_split`, индексы, FK, CHECK |
| 2026-08-01 | 1.2 Entity `CashTransactionSplit` + enum `CashTransactionSplitSource` + доменные методы на `CashTransaction` |
| 2026-08-01 | 1.3 `CashTransactionSplitRepository` (каждый метод с `companyId`) |
| 2026-08-01 | 1.4–1.6 dual-write: `CashTransactionService` (создание, редактирование), воркер автоправил, ручное применение правила в контроллере |
| 2026-08-01 | 1.7 aggregate-аудит состава строк в `CashTransactionSplitSynchronizer` |
| 2026-08-01 | 1.8–1.9 команды `app:cash:backfill-transaction-splits` и `app:cash:verify-transaction-splits` |
| 2026-08-01 | 1.10 тесты: 13 unit на инварианты набора, 7 integration на dual-write и аудит |
| 2026-08-01 | `ARCHITECTURE.md` — Entity и раздел про разбивку |

## Находки в ходе реализации

- **Девятый writer, которого не было в карте плана:** `CashTransactionAutoRuleController::applyOne` применяет правило и делает собственный flush. Подключён к синхронизатору.
- **Импорты трогать не пришлось:** три импорта создают транзакцию без категории, а строки зеркалят колонку включая её пустое состояние — инвариант держится по построению.
- **Баг, пойманный тестом:** пересоздание строки с той же категорией падало на `uniq_cts_tx_category` — Doctrine выполняет INSERT раньше DELETE в одном flush. Исправлено в корне: `replaceSplits()` переиспользует строку с совпадающей категорией. Это же чинит будущую форму мультиразбивки в Stage 4.
- **Инвариант «сумма строк = сумме транзакции» уточнён:** он применяется к непустому составу; «категории нет → строк нет» — легальное зеркало. Для D3/D4 в Stage 3 это означает LEFT JOIN, иначе транзакция без категории пропадёт из ведомости.

## Открытые вопросы

- D4: питает ли XLSX-выгрузка 1С или внешнюю таблицу — влияет на 3.3.
- Оператор и wrapper для backfill/verify/backup на проде — определить до Production Gate A.

## Stage 1 — закрыт

- Внешнее ревью: 5 итераций Codex CLI 0.146.0, результат `REVIEW_GREEN`. Разбор находок — в `stages/stage-1.md`.
- Полный прогон: 2938 тестов, 1 предсуществующее падение (`DashboardSnapshotGoldenTest`, зависит от календарной даты, доказано на `80ad3cfe`).
- Пять итераций — сигнал на будущее: инкапсуляцию агрегата я недооценил дважды подряд, итерации 3 и 4 обе про обход `replaceSplits()`. В Stage 4, где появится форма мультиразбивки, начинать надо с инвариантов, а не с UI.

## Следующее действие

Stage 2 — это Production Gate A (backfill на проде). Вход только по отдельному явному
разрешению Владельца: остановка воркеров, бэкап, backfill, verify.
