# Расшифровка удержаний WB

## Stage 1: Расшифровка raw-удержаний по основанию WB

Risk: MEDIUM
owner_gate: yes
release_candidate: yes
independently_deployable: yes
stage_base_commit: `baf8a12b7e5570a8f0e0f846293d9a5316ae8b8a`

Definition of Done:

- Общая сумма статьи «Удержания» и все действующие финансовые формулы не меняются.
- Для строк с ненулевым `deduction` отчёт показывает агрегированную расшифровку
  по точному raw-значению `bonusTypeName` / `bonus_type_name`.
- Пустое основание отображается как «Без расшифровки WB».
- Расшифровка учитывает выбранный период, `reportId` и активную компанию.
- Сумма расшифровки в точности равна общей сумме статьи «Удержания».
- Расшифровка доступна на странице и в CSV с защитой от spreadsheet formulas.
- Агрегация хранит только уникальные основания, а не все raw-строки.
- Транзакции, БД, WB sync/API, очереди и production не изменяются.

Work items:

- 1.1 — добавить агрегирование удержаний по raw-основанию в builder.
- 1.2 — вывести расшифровку на странице и в CSV.
- 1.3 — добавить unit/functional-покрытие и эксплуатационную документацию.

Stage checks:

- Целевые unit- и functional-тесты WB raw financial report.
- Релевантные Marketplace-тесты.
- PHP CS для изменённых PHP-файлов.
- Twig syntax lint и Symfony container lint.
- Проверка отсутствия обращения отчёта к финансовым транзакциям.
- Внутреннее ревью полного Stage diff.
- Внешнее read-only Claude Code review до `REVIEW_GREEN`.
- Зелёный CI Draft PR.

Reviewer focus:

- Неизменность финансовых итогов и правил знаков.
- Строгая tenant-изоляция и применение текущих фильтров.
- Точное отображение основания WB без собственной классификации.
- Ограниченная память и spreadsheet safety.

Explicitly excluded:

- Финансовые транзакции и транзакционные проекции.
- Новые бухгалтерские категории или нормализация оснований WB.
- Построчная детализация по `rrdId`, SKU и `srid`.
- Схема БД, миграции, sync/API и новые зависимости.
- Merge, deploy и любые production-действия без отдельного owner decision.
