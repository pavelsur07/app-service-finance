### Stage 6: закрытие контура — DONE

**Risk:** HIGH-LOCAL
**Owner gate:** yes (получен: «продолжай Stage 6»)
**Next action:** Final Release Gate — задача завершена

#### Scope Stage
- Stage base commit: `4f527262` (коммит Stage 5)
- Work items: 6.1 — 28 файлов `tests/`; 6.2 — включение правила в фиксере;
  6.3 — **отдельный достижимо зелёный гейт** (добавлен по результату ревью)

#### Что сделано
1. `declare(strict_types=1)` добавлен в 28 файлов `tests/`. **Долг: 28 → 0.**
2. `site/.php-cs-fixer.php`: `'declare_strict_types' => false` → `true`.
3. Создан `site/.php-cs-fixer.strict-types.php` — отдельный конфиг под одно
   правило; скрипт `composer cs:strict-types` и таргет `make site-cs-strict-types`.
4. `CLAUDE.md` приведён в соответствие фактам.

#### Definition of Done задачи
- [x] 0 файлов без объявления в `site/src` и `site/tests` (было 309)
- [x] правило включено в фиксере
- [x] **существует достижимо зелёный гейт, который краснеет при нарушении**
- [x] полный набор тестов не хуже baseline
- [x] внешний review `REVIEW_GREEN`

#### Baseline и проверка гейта
- `make site-cs-check` **до** (правило выключено): `Found 506 of 2342`, exit 8
- Из 28 целевых файлов только 1 был в этих 506 по другим правилам, поэтому
  включение флага без правки дало бы 533. Предсказание проверяемое.
- `make site-cs-check` **после**: `Found 506 of 2342`, exit 8. Список красных
  файлов совпал полностью — ни одного нового. Нарушений `declare_strict_types`
  в выводе ноль.

#### CANARY — доказательство, что гейт работает
Проведён по прямому требованию внешнего ревью:

| шаг | состояние | результат |
|---|---|---|
| 1 | чистое дерево | `Found 0 of 2342`, exit 0 |
| 2 | `declare` убран из `src/Util/StringNormalizer.php` (файл заведомо вне исходных 506) | `Found 1 of 2342`, exit 2, назван ровно этот файл |
| 3 | файл восстановлен | `Found 0 of 2342`, exit 0, `git diff` пуст |

#### Checks
- полный `make site-test` **после**: `Tests: 3461, Assertions: 20360, Deprecations: 7`, 0 падений, exit 0, 6:15 — идентично baseline
- юнит после правки `composer.json`: `Tests: 1927`, exit 0
- `make site-cs-strict-types`: `Found 0 of 2342`, exit 0

#### Внешний review — 2 круга, обе находки устранены
Ревьюер: Codex CLI 0.148.0. В промпт первого круга был **намеренно вынесен
неудобный вопрос**: не создаёт ли включённое правило ложного чувства защиты, раз
`cs-check` всё равно красный. Ответ оказался отрицательным для меня, и это самая
ценная находка всей задачи.

- **IMPORTANT 1 (устранён).** `make site-cs-check` возвращает `exit 8` и в норме,
  и при нарушении — для CI состояния неразличимы. Доказательство «506 = 506»
  показывает отсутствие текущего долга, но не то, что гейт поймает регресс.
  Формулировка в `CLAUDE.md` была слишком сильной. Это ровно антипаттерн
  «хронически красный гейт» из самого `CLAUDE.md`.
  → Устранено по существу: отдельный конфиг + скрипт + таргет + canary выше.
- **IMPORTANT 2 (устранён).** Не был приложен финальный прогон. → Приложен.
- **MINOR (исправлен).** Формулировки «в каждом файле», «любой новый файл»,
  «единственная причина» шире фактического поведения: Finder покрывает только
  `site/src` и `site/tests`, `ignoreVCSIgnored(true)` исключает игнорируемые
  пути, а ненулевой код может дать и синтаксическая ошибка.
  → Проверено предметно: вне охвата 3 файла в `site/config` и
  `site/public/index.php` без объявления (точки входа и конфиги), в
  `site/migrations` все 242 объявление имеют. Формулировки в `CLAUDE.md` и в
  шапке конфига переписаны по тексту ревьюера.
- **FOLLOW-UP (реализован).** Ветки, созданные до миграции, при `make site-cs-fix`
  получат массовое дописывание `declare` в посторонний diff — и это не
  форматирование, а изменение строгости вызовов. Предупреждение добавлено в
  `CLAUDE.md` по тексту ревьюера.

#### Риски / на что смотреть
- `make site-cs-check` остаётся красным на 506 файлах по правилам `@Symfony`.
  Это состояние существовало до задачи и ею не создано, но остаётся дефектом:
  по `CLAUDE.md` хронически красный гейт надо либо довести до зелёного, либо
  удалить. Отдельная задача, в scope не тянул.
- Три файла (`SnapshotTelemetry`, `ChargeTypeController`,
  `DashboardNotifyController`) без собственных тестов — FOLLOW-UP со Stage 5.

#### Открытые вопросы
- нет
