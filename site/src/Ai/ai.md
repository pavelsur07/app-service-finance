Круто, считаем v0.1 модуля зафиксированным ✅

Дальше логика такая: сейчас у тебя есть каркас. Нужно превратить его в **живого помощника**, который реально даёт пользу по ДДС. Я бы шёл по этапам.

---

## 1. Запитать агента реальными данными (v0.2)

Цель: чтобы `CashflowAgent` не жил на TODO, а реально считал по твоему ДДС.

**Шаги:**

1. В `CashflowAgent`:

    * вместо TODO подключить:

        * репозитории/сервисы по ДДС (`CashTransactionRepository`, `MoneyAccountDailyBalance`, `PaymentPlanService` или их аналоги в проекте);
        * собрать `CashflowAgentInput`:

            * `totalsByCategory` по статьям ДДС за последние 30 дней;
            * `dailyBalances` по счетам;
            * `upcomingPayments` (платёжный календарь ближайшие 7–14 дней);
            * `averageMonthlyRevenue/Expenses`.

2. Допилить `CashflowPromptBuilder`:

    * сделать промпт под твой реальный формат данных;
    * чётко описать, в каком **JSON-формате** LLM должен вернуть рекомендации (например, массив объектов `{title, description, severity, related_entity_type, related_entity_id}`).

3. В `CashflowAgent::run()`:

    * распарсить JSON → создать `AiSuggestion` по каждой рекомендации.

4. Сделать миграции для `AiAgent`, `AiRun`, `AiSuggestion` и прогнать их.

👉 Могу следующим шагом подготовить тебе **узкий Codex-промт только для доработки `CashflowAgent` и `CashflowPromptBuilder` под реальные классы ДДС**.

---

## 2. Сделать видимый UI-блок “AI-рекомендации”

Цель: чтобы пользователь не искал их где-то в меню.

1. В `AiSuggestionController`:

    * сделать нормальную выборку последних N рекомендаций по активной компании;
    * добавить пагинацию или хотя бы сортировку.

2. В Twig:

    * внедрить мини-блок “AI-помощник”:

        * на дашборд или в раздел ДДС;
        * метка с количеством непрочитанных;
        * кнопки “Пометить как прочитанное / применено”.

3. (Опционально) быстрый фильтр:

    * по `severity` (HIGH / MEDIUM / LOW);
    * по периоду (последние 7/30 дней).

---

## 3. Настроить запуск: cron + тестовые компании

Цель: чтобы агент работал **регулярно**, но не шумел всем.

1. Включить `RunCashflowAgentsCommand` в cron:

    * 1 раз в день, например в 03:00 ночи.

2. В `AiAgent`:

    * завести записи только для 1–2 тестовых компаний (в БД или отдельной миграцией/фикстурой).
    * включить `isEnabled = true`, `type = CASHFLOW`.

3. Прогнать вручную:

    * `php bin/console app:ai:run-cashflow-agents`
    * посмотреть:

        * записи в `ai_run`;
        * записи в `ai_suggestion`;
        * как они отображаются в UI.

4. Поиграть с промптом:

    * если рекомендации слишком общие/водянистые — ужесточить требования;
    * если их мало — расширить контекст.

---

## 4. Добавить безопасный флажок-фичу

Чтобы модуль не влиял на всех пользователей до полировки:

* в `parameters` или `.env` завести флаг типа `FEATURE_AI_AGENTS=true/false`;
* оборачивать:

    * показ блока в UI,
    * создание `AiAgent` по умолчанию,
    * запуск Cron.

Это позволит спокойно включить/выключить фичу.

---

## 5. Следующий агент: Платёжный календарь или P&L (v0.3+)

Когда Cashflow-агент будет давать ценность:

1. Добавить второй тип в `AiAgentType`:

    * `PAYMENT_CALENDAR` (агент по кассовым разрывам) **или**
    * `PNL` (агент по марже/прибыли).

2. Скопировать паттерн:

    * DTO `PaymentCalendarAgentInput` / `PnlAgentInput`;
    * `PaymentCalendarPromptBuilder` / `PnlPromptBuilder`;
    * `PaymentCalendarAgent` / `PnlAgent`.

3. На UI:

    * фильтр “Тип агента”;
    * можно сводный вид: “Финансовые риски” (кассовые разрывы + падение маржи).

---

## 6. Что я предлагаю прямо сейчас

Чтобы не размазываться, следующий конкретный шаг:

> **Шаг 1: “вживить” CashflowAgent в реальный ДДС.**

Если ок, в следующем ответе я:

* дам тебе **узкий Codex-промт**:

    * для доработки `CashflowAgent`,
    * для корректного `CashflowPromptBuilder`,
    * для добавления миграций по трём сущностям,
* без затрагивания остальных модулей, только связка с реальными репозиториями ДДС.

Если хочешь другой фокус (например, сначала UI или наоборот чат-ассистент), просто скажи, и я перестрою порядок шагов — но без уточняющих вопросов я бы пошёл именно от данных.
