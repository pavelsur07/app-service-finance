framework:
    lock:
        # Используем отдельный каталог var/lock/<env> для файловых блокировок,
        # чтобы Symfony не создавал их в рабочей директории (site).
        resources:
            default: 'flock://%kernel.project_dir%/var/lock/%kernel.environment%'
