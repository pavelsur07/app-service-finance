framework:
    # Храним файлы блокировок в каталоге кеша, который Symfony гарантированно
    # создаёт для каждого окружения. Так исключаем появление артефактов в
    # корневой директории проекта.
    lock: 'flock://%kernel.cache_dir%/locks'
