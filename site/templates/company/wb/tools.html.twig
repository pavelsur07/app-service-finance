{% extends 'base.html.twig' %}

{% block title %}WB: Тех. операции{% endblock %}

{% block body %}
<div class="container-xl">
  <div class="page-header d-print-none">
    <div class="row align-items-center">
      <div class="col">
        <h2 class="page-title">WB: Технические операции</h2>
        <div class="text-secondary">
          Активная компания: <strong>{{ company.name }}</strong>
        </div>
      </div>
    </div>
  </div>

  {% for label, messages in app.flashes %}
    {% for message in messages %}
      <div class="alert alert-{{ label }} my-2" role="alert">
        {{ message }}
      </div>
    {% endfor %}
  {% endfor %}

  <div class="row row-cards">
    <div class="col-md-6">
      <div class="card">
        <div class="card-status-top bg-primary"></div>
        <div class="card-header">
          <h3 class="card-title">Финансовые отчёты WB</h3>
        </div>
        <div class="card-body">
          <p class="text-secondary">
            Ручной запуск процедуры получения финансовых отчётов с API Wildberries
            для активной компании. Позже добавим параметры периода и просмотр статуса.
          </p>
          <form method="post" action="{{ path('company_wb_tools_run_finance') }}">
            <input type="hidden" name="_token" value="{{ csrf_finance }}">
            <button class="btn btn-primary" type="submit">
              Запустить загрузку фин. отчётов
            </button>
          </form>
        </div>
      </div>
    </div>

    <div class="col-md-6">
      <div class="card">
        <div class="card-status-top bg-green"></div>
        <div class="card-header">
          <h3 class="card-title">Продажи WB</h3>
        </div>
        <div class="card-body">
          <p class="text-secondary">
            Ручной запуск процедуры получения данных о продажах с API Wildberries
            для активной компании. Позже добавим расширенные опции.
          </p>
          <form method="post" action="{{ path('company_wb_tools_run_sales') }}">
            <input type="hidden" name="_token" value="{{ csrf_sales }}">
            <button class="btn btn-green" type="submit">
              Запустить загрузку продаж
            </button>
          </form>
        </div>
      </div>
    </div>

  </div>
</div>
{% endblock %}
