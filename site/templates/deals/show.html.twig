{% extends 'base.html.twig' %}

{% block title %}Сделка {{ deal.number }}{% endblock %}

{% block content %}
<div class="d-flex align-items-center gap-2 flex-wrap mb-3">
    <h2 class="page-title mb-0">Сделка {{ deal.number }}</h2>
    <div class="ms-auto d-print-none">
        <a href="{{ path('deal_index') }}" class="btn btn-outline-secondary">К списку</a>
        {% if deal.isDraft %}
            <form method="post" action="{{ path('deal_confirm', {id: deal.id}) }}" class="d-inline">
                <input type="hidden" name="_token" value="{{ csrf_token('deal_confirm' ~ deal.id) }}">
                <button type="submit" class="btn btn-success">Подтвердить</button>
            </form>
            <form method="post" action="{{ path('deal_cancel', {id: deal.id}) }}" class="d-inline">
                <input type="hidden" name="_token" value="{{ csrf_token('deal_cancel' ~ deal.id) }}">
                <button type="submit" class="btn btn-outline-danger">Отменить</button>
            </form>
        {% elseif deal.isConfirmed %}
            <form method="post" action="{{ path('deal_cancel', {id: deal.id}) }}" class="d-inline">
                <input type="hidden" name="_token" value="{{ csrf_token('deal_cancel' ~ deal.id) }}">
                <button type="submit" class="btn btn-outline-danger">Отменить</button>
            </form>
        {% endif %}
    </div>
</div>

<div class="card mb-4">
    <div class="card-body">
        <div class="row">
            <div class="col-md-4">
                <div class="text-muted">Дата признания</div>
                <div>{{ deal.recognizedAt ? deal.recognizedAt|date('d.m.Y') : '—' }}</div>
            </div>
            <div class="col-md-4">
                <div class="text-muted">Канал</div>
                <div>{{ deal.channel.value }}</div>
            </div>
            <div class="col-md-4">
                <div class="text-muted">Статус</div>
                <div>{{ deal.status.value }}</div>
            </div>
        </div>
        <div class="row mt-3">
            <div class="col-md-4">
                <div class="text-muted">Сумма</div>
                <div>{{ deal.totalAmount|number_format(2, ',', ' ') }}</div>
            </div>
            <div class="col-md-4">
                <div class="text-muted">Прямые расходы</div>
                <div>{{ deal.totalDirectCost|number_format(2, ',', ' ') }}</div>
            </div>
            <div class="col-md-4">
                <div class="text-muted">Маржа</div>
                <div>{{ (deal.totalAmount - deal.totalDirectCost)|number_format(2, ',', ' ') }}</div>
            </div>
        </div>
        <div class="mt-3">
            <div class="text-muted">Название</div>
            <div>{{ deal.title ?: '—' }}</div>
        </div>
    </div>
</div>

<div class="card">
    <div class="card-header">
        <ul class="nav nav-tabs card-header-tabs" data-bs-toggle="tabs">
            <li class="nav-item">
                <a href="#deal-tab-items" class="nav-link {% if activeTab == 'items' %}active{% endif %}">
                    <i class="ti ti-list"></i>
                    Позиции
                    <span class="badge bg-secondary-lt ms-1">{{ deal.items|length }}</span>
                </a>
            </li>
            <li class="nav-item">
                <a href="#deal-tab-charges" class="nav-link {% if activeTab == 'charges' %}active{% endif %}">
                    <i class="ti ti-receipt"></i>
                    Прямые расходы
                    <span class="badge bg-secondary-lt ms-1">{{ deal.charges|length }}</span>
                </a>
            </li>
            <li class="nav-item">
                <a href="#deal-tab-adjustments" class="nav-link {% if activeTab == 'adjustments' %}active{% endif %}">
                    <i class="ti ti-adjustments"></i>
                    Корректировки
                    <span class="badge bg-secondary-lt ms-1">{{ deal.adjustments|length }}</span>
                </a>
            </li>
        </ul>
    </div>
    <div class="card-body">
        <div class="tab-content">
            <div class="tab-pane {% if activeTab == 'items' %}active show{% endif %}" id="deal-tab-items">
                {% include 'deals/_tabs/items.html.twig' with {deal: deal, itemForm: itemForm} %}
            </div>

            <div class="tab-pane {% if activeTab == 'charges' %}active show{% endif %}" id="deal-tab-charges">
                {% include 'deals/_tabs/charges.html.twig' with {deal: deal, chargeForm: chargeForm} %}
            </div>

            <div class="tab-pane {% if activeTab == 'adjustments' %}active show{% endif %}" id="deal-tab-adjustments">
                {% include 'deals/_tabs/adjustments.html.twig' with {deal: deal, adjustmentForm: adjustmentForm} %}
            </div>
        </div>
    </div>
</div>
{% endblock %}
