{% extends 'report/cashflow.html.twig' %}

{% block content %}
    {% if openings is defined and openings|length == 0 %}
        <div class="card my-3">
            <div class="card-body text-center">
                <h3 class="mb-2">Отчёт недоступен — нет данных</h3>
                <p class="text-muted mb-3">Загрузите банковскую выписку в формате 1С, чтобы увидеть движения по счетам.</p>
                <a href="{{ path('cash_bank1c_import_upload') }}" class="btn btn-primary">Перейти к Импорт 1С</a>
            </div>
        </div>
    {% endif %}
    {{ parent() }}
{% endblock %}
