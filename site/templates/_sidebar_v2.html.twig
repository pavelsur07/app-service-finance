<aside class="navbar navbar-vertical navbar-expand-lg" data-bs-theme="light">
    <div class="container-fluid">
        <h1 class="navbar-brand navbar-brand-autodark">
            <a href="{{ path('app_home_index')|default('javascript:void(0)') }}" class="text-decoration-none">
                <span class="fw-bold" style="color: var(--brand);">Ваш</span> ФинДиректор
            </a>
        </h1>

        <div class="navbar-nav flex-row d-lg-none">
            <div class="nav-item">
                <a class="nav-link" data-bs-toggle="offcanvas" href="#offcanvasSidebar" role="button" aria-controls="offcanvasSidebar">
                    <i class="ti ti-menu-2"></i>
                </a>
            </div>
        </div>

        <div class="collapse navbar-collapse" id="sidebar-menu">
            <ul class="navbar-nav pt-lg-3">
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('app_home_index')|default('javascript:void(0)') }}">
                        <span class="nav-link-icon d-md-none d-lg-inline-block"><i class="ti ti-home"></i></span>
                        <span class="nav-link-title">Дашборд</span>
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('cash_transaction_index')|default('javascript:void(0)') }}">
                        <span class="nav-link-icon d-md-none d-lg-inline-block"><i class="ti ti-wallet"></i></span>
                        <span class="nav-link-title">Деньги</span>
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_home_index')|default('javascript:void(0)') }}">
                        <span class="nav-link-icon d-md-none d-lg-inline-block"><i class="ti ti-chart-bar"></i></span>
                        <span class="nav-link-title">ОПиУ</span>
                    </a>
                </li>

                <!-- Dropdown (collapse) menu item -->
                <li class="nav-item">
                    <a class="nav-link" href="#nav-reports" data-bs-toggle="collapse" role="button" aria-expanded="false" aria-controls="nav-reports">
                        <span class="nav-link-icon d-md-none d-lg-inline-block"><i class="ti ti-report-analytics"></i></span>
                        <span class="nav-link-title">Отчёты</span>
                        <span class="nav-link-arrow ms-auto"><i class="ti ti-chevron-down"></i></span>
                    </a>
                    <div class="collapse" id="nav-reports">
                        <ul class="nav nav-pills nav-sub flex-column">
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_home_index')|default('javascript:void(0)') }}">
                                    <span class="nav-link-icon d-md-none d-lg-inline-block"><i class="ti ti-calendar-stats"></i></span>
                                    <span class="nav-link-title">Платёжный календарь</span>
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_home_index')|default('javascript:void(0)') }}">
                                    <span class="nav-link-icon d-md-none d-lg-inline-block"><i class="ti ti-scale"></i></span>
                                    <span class="nav-link-title">Баланс</span>
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_home_index')|default('javascript:void(0)') }}">
                                    <span class="nav-link-icon d-md-none d-lg-inline-block"><i class="ti ti-robot"></i></span>
                                    <span class="nav-link-title">AI-отчёт</span>
                                </a>
                            </li>
                        </ul>
                    </div>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_home_index')|default('javascript:void(0)') }}">
                        <span class="nav-link-icon d-md-none d-lg-inline-block"><i class="ti ti-building"></i></span>
                        <span class="nav-link-title">Компания</span>
                    </a>
                </li>

                <li class="nav-item mt-2">
                    <div class="hr-text">Инструменты</div>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="javascript:void(0)" data-bs-toggle="modal" data-bs-target="#modal-help">
                        <span class="nav-link-icon d-md-none d-lg-inline-block"><i class="ti ti-help-circle"></i></span>
                        <span class="nav-link-title">Справка</span>
                    </a>
                </li>
            </ul>

            <div class="mt-auto pt-4 pb-3">
                <div class="card mx-2">
                    <div class="card-body">
                        <div class="d-flex align-items-center">
                            <span class="avatar me-2">{{ app.user ? (app.user.email|default('PN')|slice(0,2)|upper) : 'PN' }}</span>
                            <div class="flex-fill">
                                <div class="fw-semibold">{{ app.user ? (app.user.userIdentifier|default('Pavel')) : 'Pavel' }}</div>
                                <div class="text-muted">
                                    {% if app.user and app.user.roles is defined %}
                                        {{ app.user.roles|first }}
                                    {% else %}
                                        ROLE_ADMIN
                                    {% endif %}
                                </div>
                            </div>
                            <a class="btn btn-outline-secondary btn-icon" href="{{ path('app_home_index')|default('javascript:void(0)') }}" aria-label="Profile">
                                <i class="ti ti-user-circle"></i>
                            </a>
                        </div>
                    </div>
                </div>
            </div>

        </div><!-- /collapse -->
    </div>
</aside>
