{{ form_start(form) }}
<div class="card">
    <div class="card-body">
        {{ form_widget(form) }}
    </div>
    <div class="card-footer text-end">
        <button class="btn btn-primary">{{ button_label|default('Сохранить') }}</button>
    </div>
</div>
{{ form_end(form) }}
